# App Page Mirror Truth Sync Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Refresh the KinNoKi Labs umbrella app pages so they mirror each app's GitHub Pages homepage in content substance, add Routey, rename Visual Timer to Turn Timer, and keep press/repositioning out of pass 1.

**Architecture:** This remains a Swift Publish static site. Markdown source files under `Content/` are edited, `publish generate` regenerates `Output/`, and Cloudflare Pages serves the committed generated output. The mirror is content-level: preserve published app-page claims, headings, status language, and calls to action while adapting each source page into the existing KinNoKi Markdown theme.

**Tech Stack:** Swift Package Manager, Publish 0.8.0, Plot, Markdown content under `Content/`, static assets under `Resources/`, generated HTML under `Output/`, shell verification with `rg`, `curl`, and `pandoc`.

## Global Constraints

- Keep `/press` out of pass 1.
- Do not redesign the KinNoKi homepage or reposition the studio story.
- Do not import app-site custom CSS, scripts, decorative HTML, mockup scaffolding, or data URI artwork.
- Keep the existing `/apps/visualtimer` slug in pass 1, but label the app as Turn Timer.
- Do not add Routey-specific privacy details beyond the general KinNoKi privacy claim unless verified from Routey's own public site or repo.
- Do not copy private route data, press notes, credentials, unpublished assets, or employer-specific data into the public site.
- Do not manually edit `Output/`; regenerate it with Publish.
- Commit source and generated `Output/` together because this repo's Cloudflare Pages deployment serves committed `Output/`.
- No new dependency or third-party framework.

---

## File Structure

**Create**

- `Content/apps/routey.md` - New KinNoKi umbrella app page mirroring the Routey GitHub Pages homepage.

**Modify**

- `Content/apps/echo.md` - Refresh content-level mirror of `https://dfakkeldy.github.io/Echo/`.
- `Content/apps/macromark.md` - Refresh content-level mirror of `https://dfakkeldy.github.io/MacroMark/`.
- `Content/apps/nsmarksthespot.md` - Refresh content-level mirror of `https://dfakkeldy.github.io/ns-marks-the-spot/`.
- `Content/apps/visualtimer.md` - Keep slug, replace Visual Timer branding/content with Turn Timer homepage mirror.
- `Content/index.md` - Homepage app list includes Echo, MacroMark, NS Marks The Spot, Turn Timer, and Routey.
- `Content/support.md` - Support list uses Turn Timer and includes Routey without inventing a Routey help page.
- `Content/privacy.md` - Privacy app list uses Turn Timer and includes Routey.
- `Output/**` - Regenerated by `publish generate` or `make generate`.

**Do Not Modify**

- `Sources/KinNoKiLabsSite/Theme/KinNoKiTheme.swift` unless generation reveals a content-driven rendering bug.
- `Resources/styles.css` unless generation reveals text overflow or unreadable content caused by the mirrored Markdown.
- Any files under `Output/` by hand.

---

### Task 1: Refresh Live Source Inputs

**Files:**
- Read: `docs/superpowers/specs/2026-07-05-app-page-mirror-truth-sync-design.md`
- Read: `Content/apps/*.md`
- Temporary only: `/tmp/kinnoki-app-mirror/*.md`

**Interfaces:**
- Consumes: Live GitHub Pages URLs from the spec.
- Produces: Fresh markdown snapshots in `/tmp/kinnoki-app-mirror/` used by Tasks 2-5.

- [ ] **Step 1: Confirm repo state**

Run:

```bash
git status --short --branch
```

Expected: branch is `codex/app-page-mirror-spec` or a successor implementation branch, with no unexpected uncommitted files.

- [ ] **Step 2: Create temporary source directory**

Run:

```bash
rm -rf /tmp/kinnoki-app-mirror
mkdir -p /tmp/kinnoki-app-mirror
```

Expected: command exits 0 and no repo files change.

- [ ] **Step 3: Fetch and convert live app homepages**

Run:

```bash
curl -L -s https://dfakkeldy.github.io/Echo/ | pandoc -f html -t gfm --wrap=none > /tmp/kinnoki-app-mirror/echo.md
curl -L -s https://dfakkeldy.github.io/MacroMark/ | pandoc -f html -t gfm --wrap=none > /tmp/kinnoki-app-mirror/macromark.md
curl -L -s https://dfakkeldy.github.io/ns-marks-the-spot/ | pandoc -f html -t gfm --wrap=none > /tmp/kinnoki-app-mirror/nsmarksthespot.md
curl -L -s https://dfakkeldy.github.io/VisualTimer/ | pandoc -f html -t gfm --wrap=none > /tmp/kinnoki-app-mirror/turntimer.md
curl -L -s https://dfakkeldy.github.io/Routey/ | pandoc -f html -t gfm --wrap=none > /tmp/kinnoki-app-mirror/routey.md
```

Expected: all five files exist and are non-empty.

- [ ] **Step 4: Verify the source snapshots contain expected anchors**

Run:

```bash
rg -n "Turn listening into learning|MacroMark|NS Marks The Spot|Make time visible|Your route" /tmp/kinnoki-app-mirror
```

Expected:

- `echo.md` contains `Turn listening into learning`
- `macromark.md` contains `MacroMark`
- `nsmarksthespot.md` contains `NS Marks The Spot`
- `turntimer.md` contains `Make time visible`
- `routey.md` contains `Your route`

- [ ] **Step 5: Commit checkpoint**

No commit for Task 1. It creates only temporary files.

---

### Task 2: Add Routey And Shared Portfolio Lists

**Files:**
- Create: `Content/apps/routey.md`
- Modify: `Content/index.md`
- Modify: `Content/support.md`
- Modify: `Content/privacy.md`

**Interfaces:**
- Consumes: `/tmp/kinnoki-app-mirror/routey.md`
- Produces: `/apps/routey` source page and shared five-app list surfaces.

- [ ] **Step 1: Create `Content/apps/routey.md`**

Create `Content/apps/routey.md` with this exact Markdown, keeping the public disclaimer:

```markdown
# Routey

**Your route. Your rules.**

Routey is the offline-first iPhone companion built for rural mail carriers. Import a route, search it locally, snap parcel labels with OCR, and keep moving even where there's no cell signal.

> Internal TestFlight is active and V1.0 is in progress. The V1.0 cut focuses on the phone workflow first; watchOS and CarPlay stay planned until the phone workflow is stable, reviewed, and useful in the truck.

## The official tools weren't built for rural routes

Rural carriers handle more parcels than ever, with handhelds designed for city sidewalks — not gravel roads, dead zones, and hundred-parcel mornings.

Routey is built from the truck, not the boardroom: local search, parcel capture, and daily delivery context designed around the way a rural route actually moves.

## Built for how carriers actually work

- **OCR Snap-to-Add:** Snap a parcel label to add it quickly without typing every address by hand.
- **Master Route List:** Keep the route in delivery order so today's work can be sorted against a known backbone.
- **watchOS Companion:** Planned wrist surfaces for the next stop and quick delivery actions after the phone workflow is stable.
- **CarPlay Navigation:** Planned in-car route context after the phone workflow proves itself.
- **Flexible Route Views:** Switch between the master route and today's run so odd days do not break the system.
- **Proof of Delivery:** Record delivery context when it matters.
- **Delivery Intelligence:** Turn repeated field work into route knowledge over time.
- **Predictive Search:** Find stops quickly from local route data.

## Three steps every morning

1. **Sort your mail.** Start from the route's real delivery order instead of rebuilding the day from scratch.
2. **Snap your parcels.** OCR-scan each label to add it in route order automatically — or search and tap, whichever is faster.
3. **Deliver from Today's Run.** Work from the daily list while keeping the master route intact.

## V1.0 still has gates

- **iPhone nightly:** The current path is iPhone-first.
- **V1.0 release gates:** Route import, local search, parcel capture, and daily-run flow need to be stable before public launch claims get stronger.
- **Apple ecosystem surfaces:** watchOS and CarPlay are planned surfaces, not pass-1 public promises.

Routey is in active development with internal TestFlight builds running. Star the project to follow along while V1.0 moves toward review.

**Independent project note:** Routey is an independent project and is not affiliated with, endorsed by, or sponsored by any postal or delivery service. Apple, CarPlay, and Apple Watch are trademarks of Apple Inc.

---
[Project Site](https://dfakkeldy.github.io/Routey/) | [View Source Code on GitHub](https://github.com/dfakkeldy/Routey)
```

- [ ] **Step 2: Update `Content/index.md` app list**

Replace the `## Our Apps` list in `Content/index.md` with:

```markdown
## Our Apps

Take a look at what we've been working on:

- **[Echo](/apps/echo)**: An audiobook study player that turns listening into learning.
- **[MacroMark](/apps/macromark)**: Apple Watch voice capture for people whose notes live in Markdown.
- **[NS Marks The Spot](/apps/nsmarksthespot)**: Historical Nova Scotia maps, lined up with the map in your hand.
- **[Turn Timer](/apps/visualtimer)**: Visual rounds for real-world countdowns, turns, routines, and reusable sequences.
- **[Routey](/apps/routey)**: Offline-first route support for rural delivery workflows.
```

Keep the page title and footer links unchanged.

- [ ] **Step 3: Update `Content/support.md` app list**

Replace the support app list with:

```markdown
## App Support

- **[Echo](/echo-help)**
- **[MacroMark](/macromark-help)**
- **[NS Marks The Spot](/nsmarksthespot-help)**
- **[Turn Timer](/visualtimer-help)**
- **[Routey](/apps/routey)**
```

Do not create a Routey support page in this task.

- [ ] **Step 4: Update `Content/privacy.md` app list**

Replace the sentence beginning `Our applications (including` with:

```markdown
Our applications (including Echo, MacroMark, NS Marks The Spot, Turn Timer, and Routey) do not collect, transmit, or sell any of your personal data to us or any third parties. We do not use third-party analytics trackers or advertising frameworks.
```

- [ ] **Step 5: Verify source references**

Run:

```bash
rg -n "Routey|Visual Timer|Turn Timer" Content/index.md Content/support.md Content/privacy.md Content/apps/routey.md
```

Expected:

- `Routey` appears in all four files.
- `Turn Timer` appears in index, support, and privacy.
- `Visual Timer` does not appear in those four files.

- [ ] **Step 6: Commit**

Run:

```bash
git add Content/apps/routey.md Content/index.md Content/support.md Content/privacy.md
git commit -m "feat: add Routey to KinNoKi app portfolio"
```

Expected: commit succeeds.

---

### Task 3: Mirror Turn Timer Page In Existing Slug

**Files:**
- Modify: `Content/apps/visualtimer.md`
- Modify: `Content/visualtimer-help.md`

**Interfaces:**
- Consumes: `/tmp/kinnoki-app-mirror/turntimer.md`
- Produces: `/apps/visualtimer` labeled as Turn Timer while preserving URL compatibility.

- [ ] **Step 1: Replace `Content/apps/visualtimer.md`**

Replace the whole file with:

```markdown
# Turn Timer

**Make time visible for turns, routines, and reusable sequences.**

Turn Timer is an Apple-native visual sequence timer being prepared for App Store release. It keeps the one-tap visual countdown from the original app and adds reusable sequence structure for game nights, routines, classrooms, kitchens, meetings, and other real-world countdowns.

## Build Log

Follow the weekly build-in-public digest generated from the real commit history.

[Read the devlog](https://dfakkeldy.github.io/VisualTimer/devlog.html)

## Roadmap

See the release phases, staged branch work, and next App Store steps.

[Open the roadmap](https://github.com/dfakkeldy/VisualTimer/blob/main/Roadmap.md)

## Architecture

Review the MVVM boundaries, data formats, CloudKit notes, and release ladder.

[Open architecture notes](https://github.com/dfakkeldy/VisualTimer/blob/main/Architecture.md)

## App Store Readiness

Track metadata, privacy, screenshots, TestFlight, CloudKit, and submission blockers.

[Open readiness checklist](https://github.com/dfakkeldy/VisualTimer/blob/main/docs/app-store/readiness.md)

## Support

Get help with Turn Timer setup, Pro restore, templates, imports, exports, and app data.

[Open support](https://dfakkeldy.github.io/VisualTimer/support.html)

## Privacy

Read how Turn Timer stores timer data locally and handles optional Apple services.

[Read privacy policy](https://dfakkeldy.github.io/VisualTimer/privacy.html)

Turn Timer is being rebranded from Visual Timer. This KinNoKi page keeps the existing `/apps/visualtimer` URL for compatibility while using the current public product name.

---
[Project Site](https://dfakkeldy.github.io/VisualTimer/) | [View Source Code on GitHub](https://github.com/dfakkeldy/VisualTimer) | [Get Help & Support](/visualtimer-help)
```

- [ ] **Step 2: Update `Content/visualtimer-help.md` title and intro**

Replace:

```markdown
# Support & Help for Visual Timer

Thank you for using Visual Timer.
```

with:

```markdown
# Support & Help for Turn Timer

Thank you for using Turn Timer.
```

Run this command after the title change:

```bash
perl -0pi -e 's/Visual Timer/Turn Timer/g' Content/visualtimer-help.md
```

Expected: all product-name references in the help page use `Turn Timer`.

- [ ] **Step 3: Verify Turn Timer naming**

Run:

```bash
rg -n "Visual Timer|Turn Timer" Content/apps/visualtimer.md Content/visualtimer-help.md
```

Expected:

- `Turn Timer` appears in both files.
- `Visual Timer` appears only in the compatibility sentence in `Content/apps/visualtimer.md`.

- [ ] **Step 4: Commit**

Run:

```bash
git add Content/apps/visualtimer.md Content/visualtimer-help.md
git commit -m "fix: mirror Turn Timer app page"
```

Expected: commit succeeds.

---

### Task 4: Mirror MacroMark And NS Marks App Pages

**Files:**
- Modify: `Content/apps/macromark.md`
- Modify: `Content/apps/nsmarksthespot.md`

**Interfaces:**
- Consumes: `/tmp/kinnoki-app-mirror/macromark.md`
- Consumes: `/tmp/kinnoki-app-mirror/nsmarksthespot.md`
- Produces: refreshed compact app pages that mirror current GitHub Pages claims.

- [ ] **Step 1: Replace `Content/apps/macromark.md`**

Replace the whole file with:

```markdown
# MacroMark

**Apple Watch to Markdown.**

MacroMark is voice capture for people whose notes live in Markdown. Tap your Watch complication, speak, lower your wrist, and let MacroMark append the thought to today's note.

## A note before the thought cools

MacroMark is built around the moment when opening a phone would be enough friction to lose the idea. The Watch collects, the phone processes, and your vault receives one clean Markdown entry.

1. **Tap from your watch face.** Deep links open instant capture without a hunt through app screens.
2. **Speak, pause, keep going.** The custom audio path is designed for long, messy thoughts instead of tiny dictated snippets.
3. **Lower your wrist to save.** Recordings are queued durably on the Watch and delivered to iPhone when the connection is ready.
4. **Land in today's file.** Processed notes append to a dated Markdown file instead of scattering across throwaway documents.

## Designed for vaults, not inboxes

MacroMark writes to plain-text daily notes so capture lands where PKM users already work. The product is built for Markdown workflows, review by date, and notes that remain useful outside the app.

## Native where reliability matters

- **watchOS capture:** Capture starts from the wrist, where the thought appears.
- **WatchConnectivity:** Watch recordings queue durably and transfer when the phone is available.
- **iCloud Drive:** Notes land in user-owned files.
- **SwiftUI and SwiftData:** Native Apple-platform foundations keep the app focused and local.

## V1.0 is about trust

- **Reliability core:** Capture should survive real wrist-down usage and connectivity gaps.
- **Status and recovery:** The app should make pending and failed transfers understandable.
- **Review by date:** Captures should be easy to audit in daily-note form.
- **App Intents:** System surfaces should make capture fast.
- **Setup proof:** First-run setup should show exactly where notes go.
- **Daily-note shape:** The output should be clean enough to live in a real vault.
- **Transcription integrity:** The text should preserve what was said without surprise formatting.
- **Launch readiness:** Privacy, support, metadata, and release proof matter as much as features.

## Privacy stays plain

MacroMark is built around local capture, Apple speech services where applicable, and user-owned note files. It is not a social inbox or analytics product.

---
[Project Site](https://dfakkeldy.github.io/MacroMark/) | [View Source Code on GitHub](https://github.com/dfakkeldy/MacroMark) | [Get Help & Support](/macromark-help)
```

- [ ] **Step 2: Replace `Content/apps/nsmarksthespot.md`**

Replace the whole file with:

```markdown
# NS Marks The Spot

**Historical maps, live in your pocket.**

NS Marks The Spot lets you slide Nova Scotia's Fletcher maps over today's landscape and see old roads, lakes, and landmarks line up under your thumb.

## The v1.0 candidate is moving through TestFlight

The public app site presents NS Marks The Spot as a v1.0 candidate moving through TestFlight and App Store readiness work. Treat that as status language from the source site, not a stronger launch claim.

## Every slider pull is a little time travel

NS Marks The Spot turns archival Nova Scotia maps into a practical companion for walks, research trips, and weekend detours. Fade between the old survey and the current map to trace where a road bent, where a shoreline shifted, or where a name survived.

## Made for maps that do not behave like ordinary screenshots

- **Georeferenced history:** Historical raster tiles sit exactly where they belong over the modern map.
- **Instant transparency:** A dedicated overlay control makes comparison feel tactile, fast, and readable.
- **POI discoveries:** Waterfalls and other points of interest can appear as clean vector layers.
- **Offline-first tiles:** Disk and memory caching keep panning smooth when the best places have weak signal.

## Open-source cartography

The app is MIT licensed and structured around a map-engine boundary, so the interface can stay steady while the renderer evolves.

---
[Project Site](https://dfakkeldy.github.io/ns-marks-the-spot/) | [View Source Code on GitHub](https://github.com/dfakkeldy/ns-marks-the-spot) | [Get Help & Support](/nsmarksthespot-help)
```

- [ ] **Step 3: Verify page-specific source claims**

Run:

```bash
rg -n "Apple Watch to Markdown|A note before the thought cools|Historical maps, live in your pocket|Every slider pull" Content/apps/macromark.md Content/apps/nsmarksthespot.md
```

Expected: all four phrases appear.

- [ ] **Step 4: Commit**

Run:

```bash
git add Content/apps/macromark.md Content/apps/nsmarksthespot.md
git commit -m "fix: mirror MacroMark and NS Marks app pages"
```

Expected: commit succeeds.

---

### Task 5: Refresh Echo Page From Published Homepage

**Files:**
- Modify: `Content/apps/echo.md`

**Interfaces:**
- Consumes: `/tmp/kinnoki-app-mirror/echo.md`
- Produces: refreshed Echo app page that follows the current GitHub Pages homepage while staying readable in the KinNoKi theme.

- [ ] **Step 1: Replace Echo heading and intro**

In `Content/apps/echo.md`, replace the opening block through the first paragraph with:

```markdown
# Echo: Audiobook Study Player

**For Every Mind — turn listening into learning.**

Echo is the audiobook player that helps you *remember* what you heard — word-perfect read-along, one-tap flashcards, and a study system built on the real science of memory.
```

- [ ] **Step 2: Update the origin-story numbers and wording**

In the `## Built on a mail route` section, include this source-site wording:

```markdown
Echo's creator delivers mail — in and out of the car thirty times a shift, an aux cable, an Apple Watch, and an AuDHD brain that learns from non-fiction audiobooks in stolen, interrupted minutes. No app on the internet would loop a single chapter until it stuck, stay ready to play on a watch after a long pause, or remember *why* a bookmark was made three driveways ago.

So he built it: **956 commits in nine weeks**, nights and weekends around a full delivery route, with no formal programming background — an app for iPhone, Apple Watch, and Mac that treats audiobooks the way students treat textbooks.
```

- [ ] **Step 3: Add the read-along differentiator section**

After the `## Just here to listen?` section and before `## What Echo does`, add:

```markdown
## Read-along is where most apps stop

A growing crowd of apps will sync your audiobook to your ebook, on your device. That's the floor — Echo does it too. The difference is the two steps above it, where a book actually sticks.

- **Chapter sync — table stakes:** Match your audiobook to your ebook chapter by chapter, so you can switch between reading and listening.
- **Word-perfect read-along:** Echo follows the narrator word by word — true karaoke, tuned on-device to the real audio, not just snapped to the nearest chapter.
- **Turn it into memory:** One tap turns any passage into a spaced-repetition flashcard with its audio snippet attached, and Echo brings it back on your iPhone, Watch, and Mac until you own it.

**Sync gets you to the page. Echo gets it into your head.**
```

- [ ] **Step 4: Align current feature names with source homepage**

In the `## What Echo does` list:

- Rename `Spaced Repetition (SRS)` to `Built-in Spaced Repetition`.
- Ensure its copy mentions `FSRS-4.5 scheduling (the modern Anki scheduler)`.
- Rename `Insights` to `Insights That Are Real`.
- Ensure `Synced ePub & PDF Reader` is represented as `Synced EPUB Reader` plus a separate `PDF Companions` item.
- Keep existing `Coming in 1.0` and `Roadmap` status markers when they already exist.

Use this exact replacement text for the affected bullets:

```markdown
- **Built-in Spaced Repetition:** An Anki-style flashcard system with audio on the cards, FSRS-4.5 scheduling, daily review, and hands-free review on Apple Watch.
- **Insights That Are Real** 🚧 **Coming in 1.0:** Listening time, streaks, chapter coverage ("Ch 7 — 86%, heard 3×"), retention curves, review forecasts — honest numbers from your actual listening, never from a server.
- **Synced EPUB Reader:** The text scrolls with the narration, tap a paragraph to jump the audio, search the book and leap to the spoken moment.
- **PDF Companions:** Diagrams and page-level material can sit beside the audio, so visual context appears when the narration needs it.
```

- [ ] **Step 5: Add source-site proof/privacy close**

Before `## Learn more`, add:

```markdown
## One library, four screens, zero friction

- **Radically private:** No accounts, no analytics, no tracking, no servers.
- **Open source:** Echo is open source under the GPL-3.0 license.
- **Your files, unlocked:** Echo is built around DRM-free files you own and can see.
- **For Every Mind:** Designed for interrupted, neurodivergent attention first — and useful for everyone whose listening gets interrupted.
```

- [ ] **Step 6: Verify Echo source phrases**

Run:

```bash
rg -n "Turn listening into learning|956 commits|Read-along is where most apps stop|Built-in Spaced Repetition|Insights That Are Real|One library, four screens" Content/apps/echo.md
```

Expected: every phrase appears.

- [ ] **Step 7: Commit**

Run:

```bash
git add Content/apps/echo.md
git commit -m "fix: mirror Echo app page"
```

Expected: commit succeeds.

---

### Task 6: Generate Site And Verify Output

**Files:**
- Modify: `Output/**` through generation only.

**Interfaces:**
- Consumes: Updated `Content/` files from Tasks 2-5.
- Produces: regenerated committed static site output.

- [ ] **Step 1: Generate the site**

Run:

```bash
make generate
```

Expected: Publish completes successfully and regenerates `Output/`.

- [ ] **Step 2: Verify Routey output exists**

Run:

```bash
test -f Output/apps/routey/index.html
```

Expected: command exits 0.

- [ ] **Step 3: Verify shared output lists**

Run:

```bash
rg -n "Echo|MacroMark|NS Marks The Spot|Turn Timer|Routey" Output/index.html Output/support/index.html Output/privacy/index.html
```

Expected: all five app names appear across the generated files. `Routey` appears in all three files.

- [ ] **Step 4: Verify stale Visual Timer output**

Run:

```bash
rg -n "Visual Timer" Content Output || true
```

Expected: only compatibility/history references remain:

- `Content/apps/visualtimer.md`
- `Output/apps/visualtimer/index.html`

No homepage, support, or privacy output should use `Visual Timer`.

- [ ] **Step 5: Verify generated page titles/content**

Run:

```bash
rg -n "Your route|Apple Watch to Markdown|Historical maps, live in your pocket|Make time visible|Turn listening into learning" Output/apps
```

Expected: each mirrored app source phrase appears in generated output.

- [ ] **Step 6: Preview locally**

Run:

```bash
make preview
```

Expected: local server starts at `http://localhost:8000`. If port 8000 is already in use, stop the conflicting local preview process or run `publish run --port 8001` and use port 8001 for the same page checks. Visit:

- `http://localhost:8000/apps/routey/`
- `http://localhost:8000/apps/visualtimer/`
- `http://localhost:8000/apps/echo/`
- `http://localhost:8000/apps/macromark/`
- `http://localhost:8000/apps/nsmarksthespot/`

Stop the server with `Ctrl-C` after visual smoke-checking the pages.

- [ ] **Step 7: Commit generated output**

Run:

```bash
git add Content Output
git commit -m "chore: regenerate mirrored app pages"
```

Expected: commit succeeds.

---

### Task 7: Final PR Verification And Closeout

**Files:**
- Read: all modified files.

**Interfaces:**
- Consumes: commits from Tasks 2-6.
- Produces: pushed branch and updated PR ready for review.

- [ ] **Step 1: Run final source checks**

Run:

```bash
git diff --check HEAD~5..HEAD
git status --short --branch
```

Expected:

- `git diff --check` exits 0.
- Working tree is clean.

- [ ] **Step 2: Push the branch**

Run:

```bash
git push
```

Expected: branch pushes to the existing PR branch.

- [ ] **Step 3: Check PR status**

Run:

```bash
gh pr checks 2 --repo dfakkeldy/KinNoKiLabsSite --watch=false
```

Expected: Cloudflare Pages is pending, passing, or absent. If it fails, inspect the linked log before guessing.

- [ ] **Step 4: Final status report**

Report:

- PR URL: `https://github.com/dfakkeldy/KinNoKiLabsSite/pull/2`
- Whether `make generate` passed.
- Whether `Output/apps/routey/index.html` exists.
- Whether Cloudflare Pages is passing, pending, failed, or absent.
- Whether the worktree is clean.

---

## Self-Review

- Spec coverage: Tasks 2-5 cover Routey, Turn Timer rename, all app-page mirrors, homepage/support/privacy shared lists, and press exclusion. Task 6 covers generated output and verification. Task 7 covers PR closeout.
- Placeholder scan: checked for forbidden planning markers and vague test instructions; none remain.
- Type consistency: no new code types or functions are introduced; file paths and source URLs match the design spec.

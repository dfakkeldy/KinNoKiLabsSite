# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

We are building a static site using the Swift [Publish](https://github.com/johnsundell/publish) framework. The site is defined as a Swift executable that generates HTML from Markdown content.

## Build & Run

```bash
# Generate + commit + push (Cloudflare Pages serves the committed Output/)
make publish

# Generate Output/ only, no commit/push
make generate          # equivalent: publish generate

# Generate and serve locally at http://localhost:8000
make preview           # equivalent: publish run

# Build the Swift package / clean artifacts
swift build
make clean             # equivalent: swift package clean
```

> [!IMPORTANT]
> Do not manually edit anything inside the `Output/` directory — it is strictly generated by Swift for our Cloudflare Pages deployment.

## Architecture

- **`Package.swift`** — SPM manifest. Single executable target `KinNoKiLabsSite` depending on `Publish` (and transitively on Plot, Ink, Files, ShellOut, Sweep).
- **`Sources/KinNoKiLabsSite/main.swift`** — All core website structure and theme logic is handled here. Defines the `KinNoKiLabsSite` struct conforming to `Website`, which configures site metadata (URL, name, sections) and the publish pipeline. Currently uses the custom `.kinNoKi` theme.
- **`Sources/KinNoKiLabsSite/Theme/KinNoKiTheme.swift`** — Custom Plot-based `HTMLFactory` theme (the "metal on black" redesign). Shared class-based `<header>`/mobile menu/`<footer>` chrome wraps every page. Two rendering strategies coexist by design: the **copy-locked marketing pages** (home, `/apps`, `/apps/echo`, `/services`, `/about`, `/support`) are recreated in the theme with `Node.raw(...)` — their layout and copy live in Swift ("prototype wins" over any markdown body); the **article pages** (posts, `/privacy`, and the `echo-*` / `*-help` guides) reflow their markdown through a `.prose` container. Interaction — dark-by-default theme toggle, OpenDyslexic font toggle, mobile menu, and reveal-on-scroll — is wired by `Resources/site.js` against the emitted classes, plus an inline no-flash `<head>` snippet that sets `data-theme` before first paint. `styles.css` is the handoff bundle verbatim; theme-emitted layout/prose classes are appended below a clearly-marked "Project additions" divider.
- **`Content/`** — Markdown source files. Top-level `.md` files become pages (e.g., `about.md` → `/about`); subdirectories become sections (e.g., `Content/posts/` → `SectionID.posts`).
- **`Resources/`** — Static assets copied verbatim to the output root. `styles.css` (dark-default tokens + light override), `site.js` (theme/font/menu/reveal behavior; also documents the no-flash snippet the theme inlines), brand + app + screenshot art under `images/{brand,apps,screenshots}`, and `OpenDyslexic-Regular.otf`. `logo.png` is kept for the favicon/og:image only.

### Publish Conventions

- **Site config** lives in the `Website` conformance (`url`, `name`, `description`, `language`, sections via `SectionID` enum).
- **Sections** are declared as cases of `SectionID: String, WebsiteSectionID`. Each section maps to a subdirectory in `Content/`.
- **Item metadata** can be added via frontmatter in content files (YAML between `---` delimiters) and parsed into `ItemMetadata`.
- **Themes** wrap the site in HTML structure. Custom themes are created by conforming to `Theme` with an `HTMLFactory` (see `Theme/KinNoKiTheme.swift`). Note: `resourcePaths` on `Theme` is only for resources bundled inside a theme package — project-level assets in `Resources/` are copied automatically by Publish.
- **Publishing steps** are customizable via `publish(using:)` instead of the default `.publish()` call (e.g., to add plugins, custom deployment, or pipeline steps).

### App item frontmatter contract

- App-item **frontmatter** now drives only the individual app *item* pages (`/apps/macromark`, `/apps/routey`, …): standard `title:`, `description:`, `image:` (icon path, also og:image) plus custom `ItemMetadata` fields `accent` (hex), `tagline`, `platforms` (comma-separated), `status` (rendered as a `.status-chip` on the item page, e.g. `TestFlight beta — open`), `featured`, `iconAlt`. Values are UNQUOTED — Ink splits metadata on the first colon. Apps without `image:` render a monogram tile fallback.
- **The marketing cards are hard-coded, not frontmatter-driven.** The home "Also in the lab" grid and the `/apps` listing (copy, statuses, and the external GitHub-Pages links) live in the theme, because the design uses different copy per surface and links out to each app's project site (only Echo links internally to `/apps/echo`). Editing an app's `.md` will *not* change those cards — edit `KinNoKiTheme.swift`.
- Marketing copy lives in the theme, so the source markdown is used narrowly: `index.md` supplies only the page `<title>`/meta (hero copy is hard-coded; `index.body` is not rendered); `services.md`/`about.md`/`support.md` supply meta only (their pages are theme-rendered); `apps/echo.md`'s body is superseded by the designed Echo page (its frontmatter still feeds meta). `privacy.md`, the `echo-*`/`*-help` guides, and posts DO reflow their markdown bodies.
